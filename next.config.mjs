/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.istockphoto.com",
      "images.unsplash.com",
      "cdn.pixabay.com",
    ], // Add your image domain here
  },
};

export default nextConfig;
